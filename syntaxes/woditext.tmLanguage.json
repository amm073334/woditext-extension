{
	"$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
	"name": "woditext",
	"patterns": [
		{"include": "#keywords"},
		{"include": "#types"},
		{"include": "#wevc"},
		{"include": "#strings"},
		{"include": "#numbers"},
		{"include": "#comments"},
		{"include": "#functions"},
		{"include": "#variables"}
	],
	"repository": {
		"keywords": {
			"patterns": [{
				"name": "keyword.control.woditext",
				"match": "\\b(if|else|goto|while|loop|ループ|return|break|ループ中断|continue|ループ開始へ)\\b"
			}]
		},
        "wevc": {
			"patterns": [
				{
                    "name": "storage.type.woditext",
					"match": "\\bwevc\\b"
				}
			]
		},
        "types": {
			"patterns": [
				{
                    "name": "storage.type.built-in.primitive.woditext",
					"match": "\\b(void|int|str)\\b"
				}
			]
		},
		"strings": {
			"name": "string.quoted.double.woditext",
			"begin": "\"",
			"end": "\"",
			"patterns": [
				{
					"name": "constant.character.escape.woditext",
					"match": "\\\\."
				}
			]
		},
        "numbers": {
			"patterns": [
				{
                    "name": "constant.numeric.decimal.woditext",
					"match": "\\b[0-9]+\\b"
				}
			]
		},
        "comments": {
			"patterns": [
				{
                    "name": "comment.line.double-slash.woditext",
					"match": "//.*$"
				}
			]
		},
        "functions": {
			"patterns": [
				{
                    "name": "entity.name.function.woditext",
					"match": "[_a-zA-Z][_a-zA-Z0-9]*\\s*(?=\\(.*\\))"
				}
			]
		},
        "variables": {
			"patterns": [
				{
                    "name": "variable.woditext",
					"match": "[_a-zA-Z][_a-zA-Z0-9]*"
				}
			]
		}
	},
	"scopeName": "source.woditext"
}